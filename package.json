{
  "name": "test-standard-version",
  "version": "1.3.1",
  "description": "测试自动生成版本号包的功能",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "major-release": "npx standard-version --no-verify -t release-",
    "minor-release": "npx standard-version --no-verify -t"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard-version": "^9.3.2"
  }
}
